# AGENTS.md

## 项目定位

- `hah` 是面向 `net/http` 的 Go JSON API 边界层，聚焦请求绑定、输入治理和响应写回。
- 保持 `net/http`-first 设计，不引入新的 router 绑定、handler 生命周期抽象或框架耦合。
- Go 版本以 `go.mod` 为准，当前为 `1.25.9`。

## 包边界

- `hah`：根包 facade，对外行为需与底层契约一致。
- `bind`：path / query / header / body 绑定。
- `reqx`：request helper、Normalize、请求级规则和校验。
- `errx`：公共 HTTP 错误模型。
- `resp`：JSON 成功响应和 `application/problem+json` 错误响应。

## 工程规则

- 这是开源项目。已发布的 public API、公开错误语义、文档承诺和示例行为默认都视为稳定契约，不能轻易修改。
- 优先做兼容性变更；不要随意重命名、删除或改变既有公开行为。
- 核心功能优先保证稳定、可靠、安全和可预测；不要为了“更快”或“更巧”牺牲正确性与可读性。
- 不做缺少数据支撑的性能优化、缓存、并发化或底层技巧优化；只有在存在明确瓶颈或真实收益时才做。
- 显式遵循 `KISS` 和 `DRY`：先选最直接、最小的实现；只在重复已明确出现时抽象复用；不要为假想扩展点设计接口或泛化 helper。
- 保持改动聚焦，尽量在现有包边界内完成；遵循标准 Go 风格，避免无必要新依赖。

## 测试规则

- 显式遵循 `TDD`：新增行为时先写或先调整测试，再实现；修 bug 时先写能失败的回归测试，再修实现。
- 测试必须覆盖公开契约，至少包括成功路径、失败路径、边界条件和已知回归风险。
- 测试要可靠、稳定、可重复，避免依赖真实网络、真实时钟、随机顺序、sleep、外部服务等 flaky 因素。
- 断言要严格，失败时能明确定位行为差异；优先测公开入口，避免同一契约在多个层级重复验证。
- 完成前至少运行 `make ci`；若改动涉及核心 HTTP 边界、共享状态或并发路径，再运行 `go test ./... -race`。
- 若改动涉及解析、归一化、错误收敛或输入面，评估是否需要 `go test ./<pkg> -count=100` 或 fuzz。

## 文档与示例

- 公开 API 或默认行为变化时，同步更新相关文档与示例：`README.md`、`REQUESTS.md`、`docs/*`、`_examples/*`。

## 变更检查

- 是否保持了既有包边界和稳定 public API？
- 是否优先选择了兼容、简单、可维护的方案？
- 是否先按 `TDD` 补了全面且稳定的测试？
- 是否避免了没有证据支撑的优化或复杂化？
- 是否同步更新了相关文档和示例？
